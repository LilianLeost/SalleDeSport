{% extends 'base.html.twig' %}

{% block title %}Rubriques{% endblock %}

{% block body %}

    <div class="container">
    <h1>Rubriques</h1>
    {% for message in app.flashes('danger') %}
        <div class="alert alert-danger">{{ message }}</div>

    {% endfor %}


    <table class="table">
        <thead>
            <tr>
                <th>Libelle</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for rubrique in rubriques %}
            <tr>
                <td>{{ rubrique.libelle }}</td>
                <td>
                    <a href="{{ path('rubriques_show', {'id': rubrique.id}) }}">Détails</a><br>
                    <a href="{{ path('rubriques_edit', {'id': rubrique.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('rubriques_new') }}">Nouvelle rubrique</a>

    </div>
{% endblock %}
